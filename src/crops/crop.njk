---
layout: layout-base.njk
pagination:
  data: crops
  size: 1
  alias: crop
permalink: "crops/{{ crop.slug }}/"
title: "Growing {{ crop.name }} in Short Growing Seasons | GrowByDate"
description: "How to grow {{ crop.name | lower }} in short growing seasons. Indoor start timing, transplant windows, and fall planning notes."
canonical: "https://growbydate.com/crops/{{ crop.slug }}/"
---

<header class="header">
  <h1>Growing {{ crop.name }} in Short Growing Seasons</h1>
  <p class="tagline">{{ crop.tagline or "Practical crop-by-crop planning references." }}</p>
  <p class="lede">{{ crop.lede or crop.description }}</p>
</header>

<section class="card">
  <h2>Quick Planning Reference</h2>
  <ul>
    {% if crop.indoorStartWeeks %}
      <li>
        <strong>Indoor start:</strong>
        {{ crop.indoorStartWeeks[1] }}–{{ crop.indoorStartWeeks[0] }} weeks before {% if crop.seasonType == "warm" %}your average {% endif %}last frost{% if crop.slug == "kale" %} (optional){% endif %}
      </li>
    {% else %}
      {# Spinach-style default #}
      <li><strong>Indoor start:</strong> Not typically needed (direct sow works well)</li>
    {% endif %}

    {% if crop.transplantWeeks %}
      <li>
        <strong>Transplant{% if crop.seasonType != "warm" %} / outdoor timing{% endif %}:</strong>
        {% if crop.transplantWeeks[0] < 0 %}
          ~{{ crop.transplantWeeks[0] | abs }} weeks before to ~{{ crop.transplantWeeks[1] }} week{% if crop.transplantWeeks[1] != 1 %}s{% endif %} after last frost
        {% else %}
          ~{{ crop.transplantWeeks[0] }}–{{ crop.transplantWeeks[1] }} weeks after last frost{% if crop.slug == "peppers" %} (warm nights matter){% endif %}{% if crop.slug == "tomatoes" %} (when nights are consistently above 50°F / 10°C){% endif %}
        {% endif %}
      </li>
    {% elif crop.directSowWeeks %}
      <li>
        <strong>Transplant / outdoor timing:</strong>
        Direct sow ~{{ crop.directSowWeeks[0] | abs }}–{{ crop.directSowWeeks[1] | abs }} weeks before last frost (as soil can be worked)
      </li>
    {% endif %}

    {% if crop.daysToMaturity %}
      <li>
        <strong>Days to maturity:</strong>
        {{ crop.daysToMaturity[0] }}–{{ crop.daysToMaturity[1] }} days{% if crop.slug in ["tomatoes","peppers","broccoli"] %} depending on variety{% endif %}{% if crop.slug == "kale" or crop.slug == "spinach" %} (baby leaves sooner){% endif %}{% if crop.slug == "peppers" %}+{% endif %}
      </li>
    {% endif %}

    <li><strong>Frost tolerance:</strong> {{ crop.frostTolerance }}</li>
  </ul>
</section>

{# Crop-specific detail cards (varieties, mistakes, fall notes, etc.) #}
{% set detailsPath = "crops/" + crop.slug + "-details.njk" %}
{% include detailsPath ignore missing %}

<p class="small muted" style="margin-top:18px;">
  <a href="/crops/">← Back to Crops</a>
</p>

<p class="small muted" style="margin-top:8px;">
  <a href="/">← Back to GrowByDate</a>
</p>
