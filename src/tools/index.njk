---
layout: layout-base.njk
title: Tools
description: Planning tools for short growing seasons.
---

<section class="card">
  <h2>Tools</h2>
  <p class="small" style="opacity:0.86;">Fast, practical calculators you can use without accounts or tracking.</p>

  {%- set toolPages = collections.all | selectattr("data.tool") -%}

  <ul class="list" style="margin-top:0.75rem;">
    {%- for p in toolPages | sort(attribute="data.title") -%}
      <li class="note">
        <a href="{{ p.url }}"><strong>{{ p.data.title or p.fileSlug }}</strong></a>
        {%- if p.data.description -%}
          <div class="small" style="opacity:0.85; margin-top:0.25rem;">{{ p.data.description }}</div>
        {%- endif -%}
      </li>
    {%- endfor -%}
  </ul>

  <div class="note small" style="opacity:0.86; margin-top:0.75rem;">
    <span class="pill">Tip</span> If you’re not sure where to start, try the Seed Start Planner first.
  </div>
</section>
