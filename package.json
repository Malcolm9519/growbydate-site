{
  "name": "growbydate",
  "private": true,
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('_site', { recursive: true, force: true })\"",
    "dev": "npm run clean && npx @11ty/eleventy --serve",
"build": "npm run clean && node --max-old-space-size=8192 ./node_modules/@11ty/eleventy/cmd.cjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}